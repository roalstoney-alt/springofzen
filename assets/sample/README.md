# Local Sample Placeholder

Keep only lightweight placeholders in GitHub Pages. Daily MP3, WAV, preview,
and cover files belong in Cloudflare R2 and should be referenced from
`assets/data/tracks.json`.
